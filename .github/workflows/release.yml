name: Legacy Release Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Use fork Android + Windows release workflow
        run: |
          echo "Legacy release.yml publishing is disabled for this fork."
          echo "Use .github/workflows/fork-android-windows-release.yml instead."
